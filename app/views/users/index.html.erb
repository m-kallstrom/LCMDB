<h2>Users on This Site</h2>

<ul>
<% @users.each do |user| %>
  <li><%= link_to user.username, user %>:  <%= user.ratings.count %> <%= user.ratings.count == 1 ? "rating" : "ratings" %></li>
<% end %>
</ul>